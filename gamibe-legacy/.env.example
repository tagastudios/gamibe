# PostgreSQL connection (legacy app only — Prisma schema uses postgres)
DATABASE_URL=postgresql://user:password@localhost:5432/gamibe_legacy

NODE_ENV=development

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace_with_long_random_string

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
